import 'package:flutter/material.dart';

// ── Sage Mint (Light, Default) ────────────────────────────────────────────────
class SageMintColors {
  static const primary = Color(0xFF3D7A5E);
  static const primaryDim = Color(0xFF2E6B4F);
  static const primaryContainer = Color(0xFFBCEDD6);
  static const onPrimary = Color(0xFFFFFFFF);
  static const secondary = Color(0xFF5A8A72);
  static const surface = Color(0xFFF6FBF8);
  static const surfaceLowest = Color(0xFFFFFFFF);
  static const surfaceLow = Color(0xFFEFF7F2);
  static const surfaceContainer = Color(0xFFE5F2EA);
  static const surfaceHigh = Color(0xFFD8EBE2);
  static const surfaceHighest = Color(0xFFCBE4D9);
  static const onSurface = Color(0xFF1A3028);
  static const onSurfaceVariant = Color(0xFF4A6B5A);
  static const outlineVariant = Color(0xFF9DC4B0);
  static const error = Color(0xFFBA1A1A);
}

// ── Kinetic Obsidian (Dark) ───────────────────────────────────────────────────
class ObsidianColors {
  static const primary = Color(0xFFF3FFCA);
  static const primaryDim = Color(0xFFBEEE00);
  static const primaryContainer = Color(0xFFCAFD00);
  static const onPrimary = Color(0xFF3A4A00);
  static const secondary = Color(0xFF00E3FD);
  static const surface = Color(0xFF0E0E0E);
  static const surfaceLowest = Color(0xFF000000);
  static const surfaceLow = Color(0xFF131313);
  static const surfaceContainer = Color(0xFF1A1A1A);
  static const surfaceHigh = Color(0xFF20201F);
  static const surfaceHighest = Color(0xFF262626);
  static const onSurface = Color(0xFFFFFFFF);
  static const onSurfaceVariant = Color(0xFFADAAAB);
  static const outlineVariant = Color(0xFF484847);
  static const error = Color(0xFFFF7351);
}

// ── Minimal Mono (Monochrome Line Style) ──────────────────────────────────────
class MinimalMonoColors {
  static const background = Color(0xFFFFFFFF);
  static const surface = Color(0xFFFFFFFF);
  static const surfaceCard = Color(0xFFFFFFFF);
  static const surfaceSunken = Color(0xFFF8F8F8);
  static const ink = Color(0xFF000000);
  static const inkSecondary = Color(0xFF333333);
  static const inkMuted = Color(0xFF666666);
  static const outline = Color(0xFF000000);
  static const outlineLight = Color(0xFFE0E0E0);
  static const accent = Color(0xFF000000);
  static const onAccent = Color(0xFFFFFFFF);
  static const error = Color(0xFF000000);
}
